{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with Clang",
            "type": "shell",
            "command": "cd build && cmake .. -DOPENSSL_ROOT_DIR=/usr/local/opt/openssl -DCMAKE_BUILD_TYPE=Debug && make clean && make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}